export class Settings {

    cardViewMode: string;
    theme: string;

    constructor(cardViewMode: string, theme: string) {
        this.cardViewMode = cardViewMode;
        this.theme = theme;
    }
}